/**
 * 실제 빌드에는 쓰이지 않는 파일입니다.
 * Electron Forge는 vite.main / vite.preload / vite.renderer.config 를 각각 사용하고,
 * 이 파일은 shadcn CLI처럼 루트의 `vite.config.*` 로 프레임워크를 감지하는 도구를 위해 존재합니다.
 */
export { default } from './vite.renderer.config.mjs';
